%Find the equation of the line perpendicular to x+c=0 and passing through the point (x1, y1)
\input{smartass.tex}
\begin{document}
%%BEGIN DEF 
%definition of variables/modules used
%Parameter for PerpendicularLineModule v1(horizontal) would force program 
%to generate solution that is horizontal line.
%for  vertical see LinePerpendicularToVerticalTemplate.tex
#<
PerpendicularLineModule v1(horizontal);
#>
%%END DEF

%%BEGIN QUESTION

Find the equation of the line perpendicular to \hspace{2mm} #<v1.originalequation#> \hspace{2mm} and 
passing through the point $(#<v1.x1#>,#<v1.y1#>)$.

%%END QUESTION

%%BEGIN SOLUTION

#<v1.solution#>

%%END SOLUTION

%%BEGIN SHORTANSWER

$#<v1.equation#>$

%%END SHORTANSWER

\end{document}
