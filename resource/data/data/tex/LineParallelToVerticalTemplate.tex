%Find the equation of the line parallel to ax+c=0 and passing through the point (x1, y1)
%Unlike LinearPrallelTemplate, this question does not allow more than 3 non-zero terms in the original equation.
\input{smartass.tex}
\begin{document}
%%BEGIN DEF 
%definition of variables/modules used
%Possible parameters ParallelLineSimpleModule(vertical) - module will generate vertical lines,
%or ParallelLineSimpleModule(horizontal) - will generate horizontal lines.
#<
ParallelLineSimpleModule v1(vertical);
#>
%%END DEF

%%BEGIN QUESTION
Find the equation of the line parallel to \hspace{2mm} #<v1.originalequation#> \hspace{2mm} and 
passing through the point $(#<v1.x1#>,#<v1.y1#>)$.
%%END QUESTION

%%BEGIN SOLUTION
#<v1.findc#>
%%END SOLUTION

%%BEGIN SHORTANSWER
$#<v1.equation#>$
%%END SHORTANSWER

\end{document}
